# dubitable's website!

Run `deno task dev` to run locally

Run `deno task test` to run tests locally

Run `deno task standard.site` to apply the
[standard.site](https://standard.site/) atproto records
